# Node / Remotion build artifacts
remotion-memorial/node_modules/
remotion-memorial/.remotion/
remotion-memorial/dist/
build/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.env
.env.local
.env.*.local

# Local notes / scratch — never publish
*.scratch
notes-local/

# Project brain + research — internal docs, must NOT be served on GitHub Pages
CLAUDE.md
primer.md
hindsight.md
RESEARCH.md
מה-זה.txt
ניצול-מנוי-קלוד.md

# Identity isolation launcher — contains live Supabase access token. NEVER commit.
start_claude.bat

# Unused content (not referenced by storyboard)
# These are kept locally for film production, NOT for public deploy.
וידאו/
assets/audio/*.mp3

# Finished "המקום" amuta videos — kept in the project on disk (OneDrive-backed),
# not committed (heavy media, not part of the website).
hamakom-videos/

# Supabase secrets (memorial corner). The migrations/ and functions/ folders ARE in git.
supabase/.env
supabase/.env.local
supabase/.branches/
supabase/.temp/

# local admin dashboard + supabase backend files (never publish to Pages)
admin-leads.html
supabase/
